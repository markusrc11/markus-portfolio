// Root layout is intentionally minimal — locale-specific layout lives in app/[locale]/layout.tsx
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
